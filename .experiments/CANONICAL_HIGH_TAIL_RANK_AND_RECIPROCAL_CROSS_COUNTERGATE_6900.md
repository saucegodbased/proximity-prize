# Canonical high-tail rank bridge and reciprocal-cross countergate

## Verdict

The projective endpoint now has an exact linear-algebra interface:

```text
CanonicalHighTailDirectionIndependent(nodes,U)
  => LI_K { (coeff_n V_r)_(n >= 133120) : r in Fin 2 }
  => finrank_K(tail span) = 2.
```

This is compiled and exported on the same
`ProjectiveHighDataElevenHighEClosedLeaf`; there is no witness exchange.

However, **rank two plus the public `AlignedCrossData` equations does not
produce the missing indirect/Hankel action**.  A literal reciprocal aligned
cross has `c=1,d=0` and is much more projectively high than required: at
`deg E=2049`, every nonzero received-row direction has canonical degree at
least `260095`.  This exceeds both `133120` and the newer exact-wrap scale
`237212+2049=239261`.

The reciprocal object is an API-scope discriminator, not an inhabitant of the
full `DataElevenHighEClosedLeaf`: it supplies an actual `AlignedCrossData`
value (hence `cross` and all four source equations), but it does not construct
the enormous retained `Good` family, common centre, scalar injection, or the
rest of the weighted/pole/no-adjacent-corner leaf.  Therefore it does not kill
a consumer which genuinely uses those global same-family fields.

## Exact rank bridge

Let

```text
V_r = Lagrange.interpolate univ nodes (U r), r=0,1,
tail(V_r)(n) = coeff_n(V_r), n>=133120.
```

Lagrange interpolation is linear, so for constants `a,b`,

```text
interpolate(a U0 + b U1) = C(a) V0 + C(b) V1.
```

If `a*tail(V0)+b*tail(V1)=0`, every coefficient of the polynomial on the
right in degrees at least `133120` vanishes.  The kernel lemma
`natDegree_le_iff_coeff_eq_zero` makes its degree at most `133119`.
Projective high-tail independence then forces `a=b=0`.  Applying
`finrank_span_eq_card` gives tail-span rank exactly two.

This proof evaluates no finite field data and uses neither `decide` nor
`native_decide`.

## Why the aligned cross alone cannot consume it

The existing `PolynomialReciprocalAlignedControl6900.reciprocalCross` has

```text
U0(i) = domain(i) / E(domain(i))
U1(i) = 1 / E(domain(i))
L=1, M=-1, c=1, d=0, N=-1,
```

and satisfies the exact `AlignedCrossData.cross` identity and all four exact
source equations.  Choose the already available root-free, conjugate-coprime
power denominator `E=X^2049-alpha`, with `sigma(alpha) != alpha`.

For any `(a,b)!=(0,0)`, let `V` be the canonical interpolant of

```text
(a*x+b)/E(x)
```

on all `262144` benchmark nodes.  If `deg V < 260095`, then

```text
H = E*V - (a*X+b)
```

has degree below `2049+260095=262144` and vanishes at all `262144` distinct
nodes.  Hence `H=0`.  This says the degree-2049 polynomial `E` divides the
nonzero affine polynomial `a*X+b`, impossible.  Thus every direction has
degree at least `260095`.

This model shows that the two independent high tails can simultaneously be
generated by one low-degree rational denominator.  Linear independence is
only a two-dimensional statement; it does not assert dense support, generic
Hankel rank, a second independent recurrence, or freedom of the individual
tail coefficients.  In particular it does not remove the remaining
mixed-envelope layers merely by being combined with `cross`.

## Consequence for route selection

Do not pursue any lemma of the form

```text
tail rank 2 + AlignedCrossData.cross/source fields
  => c,d are nonconstant
```

or an unconditional Hankel-rank gain from the same premises.  The compiled
reciprocal cross refutes that API-level inference.

A viable next consumer must visibly use at least one load-bearing field which
the countergate does not supply, most plausibly:

1. the retained `Good.card >= 253511670984674103`;
2. one fixed common `centre` on seed-dependent agreement sets;
3. injectivity and Frobenius-fixedness of the scalar family;
4. the exact residual identity involving `E0,Q,c,d,scalar_gamma`; or
5. a new excess/degree threshold strong enough to close the remaining mixed
   Full187 layers directly.

The newly formalized nonpolynomial wrap bound is compatible with the hostile
model, so it is not by itself the missing extra premise.  Its value is that it
forces one actual leaf row near the top of the NTT range; converting that into
a count still requires a global same-family incidence or structured-recurrence
theorem.

## Files and trust

- `CanonicalHighTailCoefficientRank6900.lean`
  - linearity of canonical interpolation;
  - coefficient-tail linear independence;
  - exact tail-span finrank two;
  - generic affine-over-denominator root-rigidity helper.
- `ProjectiveHighDataElevenTailRankConsumer6900.lean`
  - same-witness rank-two export on the exact projective-high DataEleven leaf.
- `ProjectiveHighReciprocalCrossCountergate6900.lean`
  - degree-`260095` lower bound for every reciprocal direction;
  - exact `CanonicalHighTailDirectionIndependent` wrapper;
  - inhabited constant-`c,d` `AlignedCrossData` countergate.

All printed theorem audits contain exactly

```text
[propext, Classical.choice, Quot.sound]
```

No `sorry`, `admit`, `native_decide`, `unsafe`, or declared axiom occurs in
these sources.  The generic rank file compiled directly in about five seconds.
The DataEleven and reciprocal adapters compiled under the established V6
valuation compatibility overlay, each below the 8 GiB local cap.
