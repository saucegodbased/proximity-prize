### Follow-up: coupled targetlike connector GREEN; whole first Full187 later-Z frontier GREEN

Two exact results sharpen the fidelity STOP in the preceding update. They do
not create a 69.00 candidate, but they identify why the compact factor module
failed and upgrade the target-exact frontier calculation from two selected
rows to the complete first exported layer.

#### 1. The targetlike packet is live; intermediate contact cancellation is essential

Commit `5c33fa9` uses the same F101 chamber as the factor-module check but
changes the error direction to the exact target analogue `X^2`. It constructs
all 2,707 literal source monomials and the coupled all-node contact plus four
boundary-coordinate map for the actual packet

```text
F3 = B*(Y-Z*q_H).
```

The complete grade-at-most-6 prefix has 1,463 columns and rank 1,463. All
four packet residues have joint quotient rank four. The first grade-7 shell
has raw derivative shapes pure `(0,0)`, curvature `(0,1)`, and slope `(1,0)`.
Exact subset ablations give:

| shell shapes | individual packet defects | joint defect |
|---|---|---:|
| pure | `(1,1,1,1)` | 4 |
| curvature | `(1,1,1,1)` | 4 |
| slope | `(1,1,1,1)` | 4 |
| pure + curvature | `(1,1,1,1)` | 4 |
| slope + curvature | `(1,1,1,1)` | 4 |
| **pure + slope** | **`(0,0,0,0)`** | **0** |

Thus pure+slope is the unique inclusion-minimal first-shell subset closing
F0,F1,F2,F3 in this structured nonmatched-error chamber. A restricted exact
F3 solve selects 1,675 columns, has rank 1,673 and augmented nullity three,
and yields a literal 1,151-term representative whose re-summed contact rows
are zero and whose boundary is exactly F3:

```text
representative sha256
  91fe5c1963971bb2b5684681d07beb53fb36e42de28a1ae870b3887c7cf0e01e
canonical result sha256
  06db4dbd0730dcdbce3653b9067d2a0d1bf131d80c63d0c9205043f4e4d080d6
```

Independent capped replay matched the receipt in about 43 seconds at about
214 MiB RSS.

The three obvious centered carriers `(Y-QZ)^4 Z^3` and their R/S companions
remain RED with joint defect four. Therefore the successful object is the
*coupled connecting map*: raw shell columns may have contact, and their
contact cancels only jointly with prefix columns. Requiring every correction
generator to be contact-zero individually caused the exact rank-one miss in
the compact bicovariant module.

At Full187 scale this first shell is only 165 polynomial channels:

```text
sum_(y=0)^82 A_y(X) Y^y Z^(83-y)
+ sum_(y=0)^81 B_y(X) R Y^y Z^(82-y).
```

They encode 894,019,556 scalar coefficient columns, so the target obligation
is a filtered F[X] connecting-surjectivity theorem, not Gaussian elimination.
A ratio-faithful structured chamber with `g<e+w` is running now; the F101
GREEN alone is not a uniform theorem.

#### 2. Complete first later-Z target frontier

Commit `b86d33b` exhaustively extends the two-row result of `91265f0`. The
first strictly later passive layer exported by each P59 pivot has exactly
three live u0-free rows: ordinary q0, induced coefficient Hasse q1, and first
curvature. Across the two bands this is exactly six aggregate rows containing
all 134 physical same-key origins.

For each band, legal physical controls give the all-node diagonal block

```text
                x        y        z
ordinary        I        I        0
curvature     -29 I   -57/2 I     0
jet             0     K_actual    I
```

where the induced higher-jet operator `K_actual` is retained. The 2x2
determinant is `1/2`, so the two-band system has rank

```text
6*N = 1,572,864,  cokernel = 0
```

for arbitrary internal and cross-band induced maps. The four physical
coefficient windows exceed `2N`, so the required H0/H1 sections are legal.
Exhaustive output scanning proves that all non-diagonal exports strictly
advance one common lexicographic order `(outer Z, J-active degree)`.

This closes the *entire first later-Z frontier*, not the cascade. The section
still exports 423,134 later u0-free occurrences and 15,272,656 positive-u0
error-facing occurrences. The next target-exact gate is the earliest such
positive-u0 frontier with an explicit curvature/+S physical group; that is
where the robust nonmatched connector evidence says curvature may first be
needed.

Receipt:

```text
canonical sha256 66a43314f383122d8547d48eca6322e45fd3b5cba396943954d8489c476d57b5
script sha256    35c822794c1fc0186bb464f5834eb971c393d892829cd45f8e45138478a71df9
```

#### Current honest blockers

1. Exact target pure-Y membership/dual gate (a RED would stop every F3 lift).
2. Ratio-faithful structured first-shell connector.
3. Earliest Full187 positive-u0/error-facing frontier, including curvature.

No build, comparator run, candidate, score file, radius, or submission has
been prepared at 69.00.
