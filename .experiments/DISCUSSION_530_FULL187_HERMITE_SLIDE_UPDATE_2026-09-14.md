### Lower 6900 update: first concrete cross-origin reducer, exact q26 frontier

Correction first: the current public lower frontier is **6808** (submission
`b3284fb`); this research checkout's untouched production candidate is 6806.
Thus 6900 is 92 centibits / 901 additional error positions above the public
frontier. There is still no verifier-ready 6900 candidate.

The latest work materially changes the Full187 terminal calculation.

1. **The one-shell connector extrapolation is stopped.** At target-shaped
   ratio `q:t=2:1`, a literal F101 control is injective even after the genuine
   unsafe deletion; all four packet defects remain. The target needs the full
   passive mapping cone, not another first-shell grid (`3eec04a`). Exact
   Hilbert arithmetic confirms why: for Full187 the first complete kernel is
   dimension-forced only at the benchmark's final grade `L=2703`. The raw
   surplus is 9,757,693, and the strong terminal deletion leaves 3,291,277
   dimensions (`bd2f679`).

2. **The terminal Hasse graph is now exact.** All 72,850 structured q=0
   origins were enumerated. Every positive-Hasse same-row family has exact
   taper `width(s+j)-j=width(s)` and nonzero unitriangular diagonal
   (`d52afc7`). At charge 13, the 11 physical streams have three correlated
   operators

   ```text
   A_s = c_s - 2 H1(c_{s+1})
   B_s = c_s - 6 H1(c_{s+1}) + 12 H2(c_{s+2}) - 8 H3(c_{s+3})
   C_s = c_s - H1(c_{s+1}).
   ```

   B is a Hasse-binomial band, not an ordinary composition cube. A is
   invertible, so the isolated charge-13 block is injective and its principal
   cokernel is exactly two tapered copies, dimension 1,693,648 (`ba5d126`,
   Lean exact-sequence proof included). This rules out a local charge-13
   kernel; cancellation must be cross-origin/multi-grade.

3. **All sharp-pivot support escapes are now covered.** The old sharp-only
   induction had 70,543 live nonpivot remainder rows. A complete exact check
   finds strong Hermite/error-value capacity witnesses for every one:

   ```text
   63,094 within the terminal-safe 103 shapes
    7,449 using another Full187 shape
        0 uncovered.
   ```

   The latter witnesses all have legal lower-active realizations
   `Y^f R^r S^s Z^(L-f-r-s)` at active grades 22..44, so the terminal deletion
   does not remove them (`ef88481`). Naive scalar elimination is still false:
   the smallest pivot/capacity pair forms a 2-cycle, although its literal
   2x2 determinant is nonzero. The live task is block confluence, not support
   discovery.

4. **There is now an actual cross-origin Hermite slide.** In the correct
   thickened jet rings `A_d=F_p[X]/(X^N-1)^d`, two legal lower-active layers

   ```text
   P8(X) Y^8 R^(21-s) S^s Z^2674,
   P7(X) Y^7 R^(21-s) S^s Z^2675
   ```

   cancel every top `f=8,q<=25` row and every top `f=7,q<=26` row of the
   charge-13 streams. This is 23,562 provenance terms on 7,282 distinct rows,
   including all 80 terms in all 33 A/B/C principal rows (`249b160`). Every
   other tail is retained; this is a real quotient step, not a packet claim.

5. **The first slide failure is also exact.** Uniform `f=8,q=26` cancellation
   by the same y=8 layer is false even for input coefficient `C=1`. The unique
   depth-27 constant-jet lift has a forced nonzero coefficient at degree
   `27N-1=7,077,887`, above the legal y=8 endpoint `7,023,751`. Ordinary
   `F_p[X]/(X^N-1)` is also invalid for this problem: Hasse derivatives do not
   descend and proper coefficient windows are not modules over that ring.

The exact remaining source question is now narrow: determine whether the
`f=8,q=26` fringe is already killed in the strong-capacity quotient, then
orient the complete CRT/pivot tail components and apply the induced Schur map
to the literal packets `F0,F1,F2,F3`. The fourth packet is always the exact
partial-locator

```text
F3 = B * (Y - P - (Z-gamma) q_H),
```

never pure Z. Current parallel experiments are testing a multi-layer q26
bypass, mixed SCC/block invertibility, and packet relevance. Input on a
filtered Hermite block-confluence theorem or a q26 dual invariant would be
especially valuable.
