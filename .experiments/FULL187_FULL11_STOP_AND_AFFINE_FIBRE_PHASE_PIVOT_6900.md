# Full11 STOP and mixed affine-fibre phase pivot

Date: 2026-09-14 UTC. Scope: lower-6900 research only. Production,
submission, `score.txt`, and `radius.txt` were not changed. The accepted
production result remains 6806; no 6900 candidate exists.

## Executive verdict

Two tempting shortcuts are now closed by complete, exact finite-field gates.

1. The complete legal weighted-order-four/full11 module does not universally
   span the three locator-normal right-hand sides. In a cap-rich nonvacuous
   chamber, three explicit duals annihilate the lower source and every legal
   full11 mode while pairing nonsingularly with `(F0,F1,F2)`.
2. The small `m=5` mixed affine-fibre simplification to pure `V` and `V1`
   shapes does not extend even to `m=6`. At `m=6`, every one of the 23
   canonical pure/`V1`/`V2` shapes is load-bearing for joint containment.
   At `m=7`, the complete source through `J+1` is injective, so the proposed
   first-shell lift does not exist at all in that chamber.

The process consequence is a real pivot, not another carrier enlargement:
work with the literal joint filtered map and propagate only the four required
classes (three locator residuals plus Z1) through its transpose. A fixed
carrier basis or a canonical lower lift may no longer be treated as complete.

## 1. Complete full11 module: exact invariant STOP

Commit `cd384ce` contains the decisive cap-rich certificate. In

```text
(n,w,g,m,D,s,t,J,L)=(10,5,7,5,35,2,1,8,12),
Q=Xi_E^2, offsets=(3,5,7),
```

the lower source has rank 3400 and leaves three independent RHS classes. The
complete legal `deg p < 3e` module generated by all eleven weighted-order-four
covariants has dimension 62 per passive shift. Four shifts add 248 independent
quotient modes. Nevertheless the final defects remain `(1,1,1)`, joint 3.

The proof is stronger than a failed span calculation: three exact dual
functionals annihilate the rank-3400 base and all 248 legal carrier images.
Their pairing matrix on `(F0,F1,F2)` is

```text
[ 4 29 51 ]
[17 38 25 ]
[21 85 13 ],       det = 40 mod 101.
```

Thus all three target classes survive. The gate includes coupled cancellation
among individually illegal raw generators; it is not the earlier
selected-row, chosen-state, tight-cap, or vacuous-control failure.

The full receipt and reproduction commands are in
`F101_CAPRICH_FULL11_FORCED_RHS_INVARIANT_STOP_6900.md`. Its final hashes are

```text
canonical 2c3967b3c8bcfccdc33deeac94c6a4ed97b6e7ac0b171dcd19f486cf086dc827
script    68667be9769733d46b3bbf24097543ab8a198a3201bad34240b6e43c62624172
```

An independent tight-cap gate in
`f101_n10_full11_nonvacuous_target_quotient_gate_6900.py` agrees at `m=5`
and `m=6`: its complete legal full11 compounds add ranks 268 and 292,
respectively, while the three individual defects remain one and the joint
defect remains three. This independent script has SHA-256
`7fe1e80454483faa860ed99f4baa2ea382ac33084f52d20b4c867708f70f065f`.

## 2. Why the m5 affine result was initially interesting

In the fixed arbitrary-error-direction chamber

```text
(n,w,g,m,D,s,t,J,L)=(10,4,7,5,35,1,1,7,11),
offsets=(3,5,7),
```

the complete grade-`<=8` contact matrix has rank/nullity `2680/71`. Support
constraints were imposed only *after* taking this complete kernel, leaving all
lower grades free. Therefore the result is about the whole affine fibre of
lifts and not one solver-selected representative.

The 17 canonical centered top shapes contain all three RHS. An exhaustive
compatible ablation has a unique maximally pruned 12-shape family:

```text
Z^2 P_6(V,Z) + V1 Z^3 Q_4(V,Z),
V=Y-QZ,  V1=R-Q'Z.
```

This constrained space has dimension 17, vertical image rank 12, and hence a
five-dimensional affine lift fibre for a prescribed RHS. The exact m5 script
is `f101_m5_top_centered_affine_fibre_6900.py`:

```text
canonical 630978e46b7ad96a554072473a6abe5e6b2c33def7af85039b115c6663699591
script    a46ffb6d59f7fd43e934ca077db362c4eeca35b25ec22241c31b2cecd11b0608
runtime   19.91 s under a 4 GiB address-space cap
```

That was valid finite evidence. The process error would have been to promote
its support pattern before checking the next multiplicity in the complete
affine fibre.

## 3. m6 exposes the derivative-layer phase change

The immediate `m=6` replay uses

```text
(n,w,g,m,D,s,t,J,L)=(10,4,7,6,42,1,1,8,12)
```

and a complete contact kernel of dimension 44 (rank 4180). The full visible
25-shape top space has kernel dimension 44, vertical image rank 12, and affine
fibre dimension 32. Restricting to the 23 canonical shapes leaves dimension
21, vertical rank 12, joint target defect zero, and affine fibre dimension 9.

But the m5 simplification is sharply false:

* the canonical pure-`V`/`V1` family has constrained kernel dimension zero;
* even allowing the extra visible noncanonical `V1*V^7*Z` shape gives only
  dimension/image rank 6 and defects `(1,1,1)`, joint 3;
* the projection of the complete contact kernel onto `V2` coordinates has
  rank 38 and kernel dimension only 6, compared with rank 27 and kernel
  dimension 44 at `m=5`;
* deleting any single one of the 23 canonical shapes destroys joint target
  containment. Twenty-two deletions give joint defect 3; deleting the
  terminal pure `V^8 Z` shape gives joint defect 1.

So `V2=S-Q''Z` is not an optional cosmetic carrier at `m=6`; its constraints
consume nearly the entire complete contact kernel. This is exactly the kind
of phase change that a canonical-lift support printout would have hidden.

The complete coefficient and affine-fibre receipt is
`ABR4_M5_M6_AFFINE_FIBRE_PHASE_AND_V2_LOAD_BEARING_6900.md`; the m6 hashes
are

```text
canonical 8ba52189959a9ff2fbefb6d080a4b3d2864641a495c6d2a48da729a6b18dcab0
script    17c9c603ac200e683f3333461bd33c0805d9dcb29f6f76f3836035755587a3f7
```

## 4. m7 refutes uniform first-shell closure

The next exact gate uses

```text
(n,w,g,m,D,s,t,J,L)=(10,4,7,7,49,1,1,9,13),
offsets=(3,5,7).
```

The complete literal ranks are

| maximum grade | columns | contact rank | nullity | RHS defects |
|---:|---:|---:|---:|:---:|
| 9 (`J`) | 5260 | 5260 | 0 | `(1,1,1)`, joint 3 |
| 10 (`J+1`) | 6119 | 6119 | 0 | `(1,1,1)`, joint 3 |

Thus the structural extractor's attempted first-shell lift failed for a
mathematical reason: no nonzero contact cycle exists in this prefix. This is
a dimension-phase counterexample to a uniform m5/m6 extrapolation, not
evidence that literal Full187 is impossible. The target is in a very different
large-surplus chamber.

The exact executable is
`f101_n10_m7_first_shell_target_gate_6900.py`:

```text
canonical a65d2dbd8fa7a0c5a9cc1351b69662f509ff95bfcbf7110425cde84ced54be02
script    77fa70879dd77529b550438d68b61506b155085f096d2cbabebfca8ef399f424
```

## 5. Surviving target facts and next falsifier

The failures do not erase the exact target interface:

```text
U = ker C_G,
W = ker(J|U),
D = C_E|W,
need C_E(F_i) in range(D), i=0,1,2,
and independently need the Z1 class outside range(D,J).
```

The abstract mapping-cone and terminal-Schur statements are already formalized
in `GlobalO2RawGradedMappingConeGate6900.lean` and
`GlobalO2TerminalCokernelSchurGate6900.lean`. The literal Full187 source has
`(m,s,t,J,L)=(60,21,10,82,2703)` and 187 derivative shapes. Grade 81 has
margin `-37,732,220,141`; grade 82 changes to surplus `9,757,693`, so the
terminal shell is genuinely load-bearing. The local contact ideal, global CRT
description, Hermite recurrences, and structured locator heads also remain
valid.

The highest-information next gate is therefore target-specific and dual:

1. form the literal 187-channel transition graph with physical
   `(active,seed,X-width)` indices and exact strip
   `0 <= a < 60g-wy-(w-1)r-(w-2)s`;
2. seed it with the three compressed locator heads
   `Lambda^59`, `(-Lambda^58 Lambda',Lambda^59)`, and
   `(Lambda^57(2 Lambda'^2-Lambda Lambda''),
   -2 Lambda^58 Lambda',Lambda^59)`, plus the independent Z1 impulse;
3. propagate only these four transpose residues through the complete filtered
   mapping cone;
4. return either a symbolic annihilation proof or an explicit legal dual
   separator.

A reachability GREEN is only permission to attempt coefficient propagation;
it is not containment. A closed cut, exhausted width, or nonzero reduced
four-residue normal form is an immediate rigorous STOP. No more time should be
spent enlarging a fixed order-four carrier list or extrapolating m5 support.

## 6. Process changes enforced

* Every proposed family must pass a complete-affine-fibre next-parameter gate
  before target formalization.
* Canonical lifts are discovery aids only; support claims must quantify over
  the full correction fibre.
* A span failure is upgraded to a dual certificate whenever possible.
* Target width and structured RHS heads must replace tiny-control position
  counts before a target inference is made.
* Production stays frozen until THREE-RHS, Z1, source legality, axiom audit,
  and capped build all pass independently.
