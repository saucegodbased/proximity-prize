# Full187 grade-seven shell: W4 projection GO, unconditional source STOP

This lower-6900 research receipt tests the canonical grade-seven shell

```text
V^2 Z^3 (c V^2 + B V Z + A Lambda Xi V1 Z),
V=Y-Xi^2Z,  V1=R-2XiXi'Z.
```

It is not a production edit or a 6900 candidate.  The exact script is
`full187_grade7_shell_w4_probe_6900.py`.

## W4 discriminator result

In the existing relay W4 quotient, set the passive seed `Z` to its local unit
and retain the leading longitudinal class of `Xi*V1` as `x`.  The first two
centered carriers project to `V^4` and `V^3`; the third projects to
`xV^2=V^3-V^2`.

For the old two duals `(D4,Dz)`, their first two columns are

```text
V^4 : (367290,-56),
V^3 : (395010,-57).
```

Their determinant is `1185030`, nonzero in characteristic `2130706433`.
Thus each single carrier lowers the old `5/6` rank defect only to `6/7`, but
the pair makes the W4 projected matrix `7/7`.  One exact projected amplitude
vector in the order
`R57,R58,R59,R60,Phi2,Phi3,V4Z3,V3Z4` is

```text
(-5015/81, 270928/1485, -9853/55, 4962214/84645,
 -334/84645, 0, -3304/1485, 272639/84645).
```

So the prior six-row W4 obstruction is genuinely broken in this precise
projection.  The third carrier is not needed for projected rank, though it
may be required by the full passive-normal shell.

## Literal source ceilings

No unproved cancellation among `c,B,A` is used in this strip calculation.
Expanding every `V=Y-Xi^2Z` tail gives the strict degree requirements

```text
deg(c) < 10170932,       from c V^4Z^3,
deg(B) < 10334394,       from B V^3Z^4,
deg(A) < 10072251,       from the seed tail of A Lambda Xi V1 V^2 Z^4.
```

The `R` tail of the third carrier is weaker (`deg(A)<10104642`); its
`V1` seed term is decisive.  For comparison, the naive agreement payments
`c=Lambda^56` and `B=Lambda^57` pass with margins `67804` and `50853`, while
`A=Lambda^56` is source-red by `30877`.

Therefore the shell is not yet an unconditional target-scale source family.
The observed centered absence of `C0,C1` does not, by itself, prove the
coefficientwise X-strip cancellation needed after returning to the literal
`Y,R,S,Z` source coordinates.  A target-scale relation/bound for `c,B,A` is
the first missing condition.

The projection also does not replace the full error-contact map: passive
seed layers, the remaining `R,S` components, coefficient Hasse jets, and
the three-RHS coupling remain to be checked.
