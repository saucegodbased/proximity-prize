# Full187 grouped-slide physical block confluence: exact STOP

Date: 2026-09-14

This is the source-provenance audit requested after the maximal complete-depth
Pascal/Hermite projection in commit `2d678e8`.  It is an experiment only.  It
does not edit a submission or assert four-packet containment.

## Exact object audited

The projection leaves 126,315 fixed-Hasse, full-contact physical source blocks
among 11,220 coefficient-polynomial shapes.  A source key is

```text
(activeDegree d, contact exponent y, R exponent r, S exponent s,
 Hasse order q, passiveSeed)
```

and the full raw contact column, without collapsing its coefficient window or
CRT provenance, is

```text
Z^q (E + Z R - Z^2 S/2)^y R^r S^s.
```

All coefficients are computed exactly in the target field
`F_2130706433`.  The 80 active-degree sectors are independent.  The script
constructs each complete sparse support, performs deterministic Hopcroft-Karp
matching, SCC-decomposes the selected pivot graph, computes every diagonal SCC
rank and determinant, and computes the rank of the complete rectangular sector
matrix using `python-flint`.

## Result

```text
physical source-block columns       126,315
aggregated contact rows              98,862
nonzero entries                    2,622,661
structural rank                      62,797
structural row defect                36,065
exact target-field rank              62,563
exact cokernel dimension             36,299
extra algebraic defect over Hall         234
selected pivot-graph SCCs            25,221
largest SCC                              100
singular diagonal SCCs                  689
sum diagonal SCC nullities             1,902
peak RSS                              275,132 KiB
```

Thus the raw capacity columns are not row-surjective.  Arbitrary independent
rowwise cancellation cannot be assembled from these columns, even though each
individual residual origin has strong capacity.  The exact SCC size, rank, and
determinant distributions are emitted by the executable receipt.

## Smallest literal Hall/cokernel witness

At active degree 11, the two rows

```text
S row: (T,E,R,S,seed) = (37,0,0,11,71)
E row: (T,E,R,S,seed) = (35,1,0,10,71)
```

have exactly one neighboring residual source:

```text
(d,y,r,s,q,seed) = (11,1,0,10,35,71)
                  = Z^35 (E + ZR - Z^2 S/2) S^10.
```

Its coefficients on `(S row,E row)` are `(-1/2,1)`.  Therefore the row-dual
vector `(2,1)` annihilates this source by the exact identity

```text
2*(-1/2) + 1 = 0.
```

Every other residual source is zero on both rows, so this is an explicit
global cokernel vector, not only a cardinality argument.

## Smallest singular selected SCC

The first singular SCC has size two at active degree 12.  Its selected rows are

```text
(38,0,1,11,70)
(36,1,1,10,70)
```

and selected source keys are

```text
(12,1,1,10,36,70)
(12,2,0,10,35,70).
```

The exact matrix is

```text
[ -1/2  -1 ]
[    1   2 ]
```

so its rank is one and determinant is zero.  This confirms that raw pivot
cycles are not removed merely by passing to the smallest block SCCs.

## Interpretation and next gate

This is a STOP for the proposed *arbitrary rowwise* residual-capacity section,
not a proof that the actual Pascal residual is nonzero or uncancellable.  The
actual target residual is itself generated by shared physical coefficient
polynomials and may automatically satisfy all 36,299 row relations.  The next
decisive computation is therefore to evaluate the explicit two-row dual (then
the full cokernel if needed) on the actual block-correlated residual in the
terminal `C/P` higher-jet symbols.  If the pairing is nonzero symbolically the
route is RED; if it vanishes identically, the physical correlations rescue the
first obstruction and the reducer must remain at coefficient-polynomial level.

The older `ef88481` sharp-cover assignments also deduplicate from 70,543 rows
to only 3,185 physical source jets.  Their fixed-q square block happens to be
invertible (3,185 singleton SCCs, determinant one), but that route is
superseded by `2d678e8` and does not dispose of other Hasse jets, passive/u0
tails, beta columns, or coefficient-polynomial compatibility.

## Reproduction

```bash
cd /home/g/proximity-prize-vm-clean-20260901/yukon-6900-work
prlimit --as=3221225472 --cpu=1200 -- \
  python3 -B .experiments/full187_grouped_slide_block_confluence_6900.py \
  --skip-old-3185
```

Observed exact receipt facts:

```text
decision = RED_RESIDUAL_RAW_BLOCK_NOT_ROW_SURJECTIVE
matching receipt sha256 = a963dcd495d9f70af782ebeda689d88f168da11617ec27008dfe606a1c19191c
full sector/SCC receipt sha256 = f3bf9139d97be6c80e359cbae7ab0af65c4726243b3508f266afbb283cc49d6c
canonical sha256 = f2e85403f4ccb8f05d082d95098c3f43fd45051911249a7b7ee6d1e59681d088
script sha256 = 1efb066722a993420ee1a73ab6018d9c5493e78af314866d0ef97e61cde0d966
```

The experiment stayed far below both the requested 4 GiB audit ceiling and
the 24 GiB verifier ceiling.
