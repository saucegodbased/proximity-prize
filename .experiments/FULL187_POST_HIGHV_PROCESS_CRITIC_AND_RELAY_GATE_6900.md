# Full187 post-high-`V` process critic and relay gate

Date: 2026-09-13 UTC. Scope: lower 6900 only. This is an adversarial process
audit of commits `421bc3d`, `a6ec329`, `1e70c456`, and `7673ed8`, together
with the corrected-width follow-up `9090e20`. It changes no production or
submission file.

## Verdict

**Stop treating autonomous high-`V` packet construction as the live proof
lane.** It is not a theorem-level target impossibility, but it is dead as a
productive search program. High-`V` rows remain plausible only as one block
of a genuinely coupled low/high Schur relay.

This distinction matters. The faithful small control says exactly:

```text
high active block alone misses F0, F1, F2;
high passive-seed block alone misses F0, F1, F2;
the whole low+high source hits F0, F1, F2.
```

The last cycle then closed progressively larger autonomous high-`V` classes:

| artifact | exact consequence | what it does not prove |
|---|---|---|
| `421bc3d` | arbitrary packets at one `H` stratum and one homogeneous normal degree are source-red | mixed strata/degrees or a low relay |
| `1e70c456` | the adjacent two-column cross-stratum syzygy either leaves a fatal remainder or collapses to one red shifted packet | three packets, `J1/J2`, seed shifts, or a low relay |
| `9090e20` | every nonzero-width pure-`V` lane is injective in the corrected ratio-faithful scaled top-error system | target injectivity or mixed normal channels |
| `7673ed8` | the exact remaining producer is THREE-RHS plus independent Z1 dual noncontainment | any literal source theorem establishing either condition |

The evidence is now one-sided enough that another inverse packet, smoothstep,
adjacent plaquette, pure-`V` schedule, or fixed-degree Bernstein combination
has low information value. A new high row is worth testing only if its design
already includes the low return block which the positive whole-source control
shows to be necessary.

## Process failures in the last cycle

1. **Parallel slots were correlated.** Three branches explored nested versions
   of the same high-`V` endpoint idea. They produced valid STOPs, but did not
   independently attack the observed low/high coupling. Parallelism increased
   receipt count more than solution-space coverage.

2. **Literal support was checked after rank.** `a6ec329` initially used only
   the active cutoff and omitted the narrower pure-seed `Q^b Z^b` cutoff. The
   apparent high-lane kernels were therefore made from illegal columns.
   `9090e20` correctly takes the minimum of both widths; all six schedules and
   their complete pure-`V` union then become injective already in the top-error
   rows. Future rank scripts must generate columns through the literal source
   predicate first, and assert equality with an independent source census,
   before any rank is interpreted.

3. **One arithmetic receipt was called sharp without optimizing its own
   hypotheses.** In `421bc3d`, `4,802,094` is a valid red corner, not the best
   relaxed corner. Under the displayed hypotheses the actual minimum deficit
   is `3,785,094`, attained at `(a,r,j,n)=(60,0,66,66)`. The STOP remains very
   safely true, but prose should quote only bounds connected to a proved
   minimization theorem.

4. **Interface work was at risk of being counted as producer progress.** The
   dual equivalences in `7673ed8` are valuable because they state the smallest
   exact goal. They are source-agnostic linear algebra, however, and do not
   reduce the literal source gap by themselves. Progress should be counted
   only when a literal source family eliminates a dual certificate or supplies
   one of the three fixed corrections/Z1.

5. **Scaled RED was allowed to steer a target claim too strongly.** The
   corrected pure-`V` calculation is excellent falsifier evidence, but it uses
   a generic five/eleven-root pair and loses the target's thin `b=66` lane.
   It justifies freezing autonomous pure-`V` search, not asserting target
   injectivity.

## Highest-information next experiment

Run one exact **dual-obstruction breaker census** in the already frozen F101
`Q=Xi_E^2` whole-source fixture. Do not search for another primal formula and
do not build a denser target matrix.

Use the exact bordered formulation already used by the successful fixed-RHS
test. For a source subset `S`, let

```text
A_S = the literal (complete contact, polynomial Y/R/S normal) column matrix,
B   = the three exact locator-normal right-hand sides F0,F1,F2.
```

Start with the boundary-degree-at-least-two high block `H`, for which all
three columns of `B` are outside `range A_H`. Compute the exact obstruction
space

```text
L_H = ker(A_H^T),
obstruction defect = rank(L_H^T B)
                   = rank([A_H | B]) - rank(A_H).
```

Partition the omitted literal columns into translation-stable blocks indexed
by `(boundary degree, seed degree, derivative pair (r,s))`, always keeping all
allowed X multipliers of a block together. For each block `G`, compute the
new exact defect for `A_(H+G)`. Then perform deterministic deletion from the
full low complement to obtain an inclusion-minimal union of blocks whose
defect is zero. Emit both the successive left-nullspace witnesses and the
primal support of a correction after each defect drop.

This is the maximum-information discriminator because every outcome changes
the plan:

* a small shared family of relay blocks identifies the missing low/high
  mechanism and gives a concrete symbolic identity to lift;
* different relay blocks for the three RHS show that a common crossbar is the
  wrong abstraction but still provide three specific lift targets;
* a diffuse, order-dependent, field-specific relay confirms that the small
  GREEN is dense linear algebra and kills formula-mining from that fixture;
* failure of the full complement to reach defect zero is an implementation
  mismatch with the frozen positive receipt and must be fixed before any new
  mathematics is trusted.

The exact theorem this experiment is trying to expose is already fixed by
`7673ed8`: for literal `V=ker C_G`, every `lambda,mu` satisfying

```text
C_E^*(lambda) = J_YRS^*(mu)
```

must satisfy `lambda(C_E(F_i))=0` for `i=0,1,2`. A relay block is useful only
if its polynomial identity visibly forces one of these three equalities.

## Three-step go/stop schedule

### 1. Extract the relay, once

Run the F101 breaker census above with exact ranks and immutable hashes.

**GO:** it produces a node-independent, translation-stable block pattern and
an explicit defect-dropping identity. **STOP:** if solvability needs a diffuse
fraction of unrelated blocks, special node values, or arbitrary individual X
columns. In the STOP case, freeze all constructive packet mining from this
fixture; retain it only as existence evidence.

### 2. Test the pattern at target arithmetic before Lean

For a GO pattern, write its unequal-modulus `H`-adic/Pascal recurrence and
check every literal head and seed tail at `(m,g,e,w)=(60,180413,81731,131071)`.
The check must include all collision terms, the `b=60` seam if reached, and
the exact multiplier widths. It must output either a universal triangular
pivot with positive strict margins or a concrete deficit/cokernel witness.

**GO:** a finite list of pivots uses only units guaranteed by squarefreeness,
maximality, and retained badness and closes all three fixed RHS. **STOP:** any
generic-rank assumption, target-sized dense elimination, omitted collision,
or nonpositive literal margin. A STOP here freezes this relay family rather
than spawning variants of the same recurrence.

### 3. Formalize the producer, then test Z1

Only after Step 2 is green, formalize the three corrections and instantiate
`threeCorrections_iff_threeRHSAnnihilator`. Immediately run the independent
Z1 noncontainment gate; do not call YRS3 a 6900 producer if Z1 is still open.

**GO:** THREE-RHS and Z1 both compile axiom-cleanly against the literal source,
then hand the four rows to the already-green base-change/root-forcing chain.
**STOP:** if THREE-RHS fails, pivot the 6900 effort to the weaker target T2
producer; if only Z1 fails, search specifically for a Z detector and keep the
three-RHS theorem as a partial artifact.

## Scheduling correction

Use at most one slot on pure/high-`V` closure. The other live slots should be
orthogonal: one exact dual-relay extractor, one target shifted/Hermite ledger
falsifier, and one Z1/downstream integration critic. Every branch must name
its binary gate before computing. The next progress metric is not another
STOP count; it is reduction of the exact THREE-RHS/Z1 defect.

