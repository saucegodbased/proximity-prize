# k=0 constant-T last-conormal killer and target scaling STOP

Date: 2026-09-14 UTC. Scope: lower-6900 exact-`G`, starting from `25f0880`.
This note changes no production file, score, candidate, or submission.

## Verdict

The finite constant-`T` obstruction is now localized exactly.

In the faithful `F_11`, two-error chamber, all ten legal partial-locator
packets have contact/augmented ranks

```text
297 / 300,
```

and leave the single compatible boundary line generated by

```text
lambda = (1,8,2,1) in coordinates (Y,R,S,Z).
```

Among unions of complete raw `X`-bands, the smallest repair has 20 raw
columns. There are exactly two minimum repairs:

```text
span { X^j Y^5, X^j Y^5 Z   : 0 <= j < 10 },
span { X^j Y^5, X^j Y^5 Z^2 : 0 <= j < 10 }.
```

Adjoining either family changes the quotient ranks by

```text
contact increment / augmented increment = 19 / 20,
```

so the normal gain becomes four and the residual line is killed. This is a
pivot-free relative-contact circuit, not four separately chosen coordinate
witnesses.

The target-scaled analogue is source-legal but **not capacity-positive on its
own**. At m=47 it has 4,376,006 coefficients, while even the error-only
`2m=94` sum-rank cap is larger by 3,306,708.

More decisively, two new target-ratio constant-`T` controls refute the direct
transport. Their all-anchor packets have boundary gain **one**, not three,
and adjoining every legal consecutive high-`Y` seed band changes contact and
augmented ranks equally. Thus the finite line and its 20-column killer are
chamber-specific. The remaining target theorem must handle a packet-compatible
dual space of variable dimension and construct enough relative raw repairs to
separate it. Neither premise is currently proved.

## 1. Exact minimum in the fixed adversarial chamber

The audit script is

```text
.experiments/k0_constantT_last_conormal_killer_6900.py
```

It uses the same fixed datum as the committed packet ablation:

```text
(n,w,g,m,B,s,U,L,k,n0)=(7,2,5,4,2,1,7,7,0,1),
field F_11,
Q=C(X,3),
two errors with delta=epsilon=1,
all ten size-three agreement anchors.
```

Every raw source monomial is literal and passes the exact relaxed-source
support predicate. The computation retains the complete raw contact-row
universe, including rows on which the packet is zero. This matters: after a
new family is adjoined, a compatible contact covector may acquire coefficients
on those new rows.

Let `C_P` and `[C_P;beta_P]` be the packet contact and augmented matrices.
Rather than selecting pivot rows, the script takes their left nullspaces and
projects every raw column into the two cokernels. For a raw family `F`,

```text
dC = rank(C_P + F)       - rank(C_P),
dA = rank([C_P;beta_P]+F)- rank([C_P;beta_P]).
```

The new boundary gain is exactly `3+dA-dC`. This gives independent quotient
controls

```text
packet contact / augmented                 297 / 300
complete source contact / augmented       1280 / 1284
packet contact / augmented left nullity   1180 / 1181
```

and the full raw source gives gain four as expected.

### Why 20 is genuinely minimum in the complete-X-band class

The 1,728 raw monomials form 113 complete `X`-bands indexed by fixed
`(Y,R,S,Z)` exponents.

The audit proves all of the following exactly:

```text
all 1,728 singleton raw monomials tested                 none kills
all 113 singleton complete X-bands tested                none kills
all 210 two-band unions having at most 20 columns tested
minimum killing union                                    20 columns
three smallest band sizes                                6,7,8
```

Since `6+7+8=21`, a union of three or more complete bands cannot beat 20.
Thus the displayed pair is globally minimum by raw-column count among unions
of complete `X`-bands; this is not merely a minimum inside active degree five.

The other minimum replaces `Z` by `Z^2`. No pair below 20 works.

### Useful larger-family controls

Each whole raw derivative-shape family independently kills the line:

```text
(R-degree,S-degree)    raw columns    dC/dA
(0,0)                       552       358/359
(0,1)                       448       336/337
(1,0)                       420       315/316
(2,0)                       308       293/294
```

By active degree, the first killing shells are degrees four and five:

```text
active degree 4: 212 columns, dC/dA=193/194
active degree 5: 135 columns, dC/dA=130/131
```

Degrees zero through three and six through seven leave gain three. These
controls show that the missing information is a relative contact circuit;
it is not intrinsically tied to adding an `S` column. The packet already
contains mixed osculating information, and a raw value/seed family can expose
the last incompatibility by matching a packet contact trace with a different
boundary trace.

Receipt:

```text
canonical SHA-256  cbba0cbaa696af58ec630bca86734e6bd159725f82b455b7ecb1289ca73328d9
script SHA-256     c4edd12c3d0ab8edd5444051b8ed05be4d6af6fbd4fbc0bc1438d59afae7c94b
runtime            22.9 s, 263 MiB peak RSS, 4 GiB process cap
```

## 2. Target-ratio transport falsifier

The feature shared by the finite `Y^5` repair and target `Y^48` is

```text
Y-degree = U-B,
```

not `m+1`; those happen to coincide only in the first m4 chamber.

`.experiments/k0_target_ratio_high_y_relative_killer_6900.py` rebuilds all
legal anchor packets in the two deterministic controls from
`k0_target_ratio_constant_t_gate_6900.py`. It first reduces the raw packet
polynomials to an exact source-span basis, then measures complete contact and
boundary ranks after adjoining

```text
{ X^j Y^(U-B) Z^z : 0 <= z <= q }.
```

The exact m5 target-ratio profile gives:

```text
raw packet columns / span dimension           8568 / 730
packet contact / augmented / gain              713 / 714 / 1
q=0 family increment                            10 / 10
q=1 family increment                            20 / 20
q=2 family increment                            30 / 30
```

The ceiling m6 profile gives:

```text
raw packet columns / span dimension          11424 / 988
packet contact / augmented / gain              979 / 980 / 1
q=0 family increment                            12 / 12
q=1 family increment                            24 / 24
q=2 family increment                            36 / 36
```

In both profiles `U-B=6`, and `q=2` exhausts the legal seed range because
`L=8`; `q=3` is source-illegal. Every high-`Y` family has zero boundary map,
is globally injective, and has the same rank already on the error rows. It
therefore kills **no** packet-compatible covector. In the ceiling profile its
agreement-row rank is smaller (12,24,36), but the error rows alone keep ranks
full (18,36,54), so agreement vanishing does not create the required relative
circuit.

This is a genuine transport STOP:

```text
F11 m4 all-anchor packet leaves one line                   GREEN
F101 target-ratio m5/m6 packet leaves three dimensions     RED for PACKET-LINE
F11 m4 U-B pair kills its line                              GREEN
F101 target-ratio m5/m6 U-B consecutive pairs/triples      RED for transport
```

It does not contradict the complete-source rank-four result in either new
control. It shows that the missing directions live in other full-source
families and cannot be inferred from the m4 circuit.

Receipt:

```text
canonical SHA-256  6c02778c2a3d582a7acf9a4e8a4cd0efc0fd0b4cacc8368eaf0e78443b483a67
script SHA-256     5a4bb1f0071047a98eeaf97f669a2e961523aa8d9b9875713917e654fee24e0d
runtime            86.3 s, 604 MiB peak RSS, 7 GiB process cap
```

## 3. Strongest general statement extracted from the certificate

`.experiments/K0LineThenKill6900.lean` proves a source-agnostic,
rank-adaptive line-then-kill theorem.

Let

```text
C    : Source -> Contact,
beta : Source -> Boundary,
P    <= Source
```

be the complete contact map, boundary map, and packet subspace. Assume:

```text
PACKET-LINE:
  every lambda compatible with C|P and beta|P equals a * lambda0;

RELATIVE-KILLER:
  there are rawRepair in Source and packetRepair in P such that
  C(rawRepair)=C(packetRepair), and
  lambda0(beta(rawRepair)-beta(packetRepair)) != 0.
```

Then the complete normal map

```text
ker C -> Boundary
```

is surjective. The proof subtracts the two repairs to obtain one vector in
`ker C`. Any complete compatible covector lies on the packet line, while
evaluation on that kernel vector forces its scalar coefficient to vanish.

The compiled theorem names include the line-specialized interface:

```text
normal_surjective_of_compatible_dual_zero
compatible_dual_zero_of_packet_line_and_kernel_killer
compatible_dual_zero_of_packet_line_and_relative_killer
normal_surjective_of_packet_line_and_relative_killer
```

and an arbitrary-corank replacement forced by the new controls:

```text
compatible_dual_zero_of_packet_separating_kernel_family
compatible_dual_zero_of_packet_separating_relative_family
normal_surjective_of_packet_separating_relative_family
```

The latter assumes no line. It takes any indexed family of raw/packet repair
pairs with equal contact traces and asks only that their boundary differences
separate the entire packet-compatible dual space. It then proves the complete
normal map surjective. Thus the abstract interface remains valid whether the
packet leaves one direction, three directions, or a rank which varies with
the mismatch stratum.

No finite-dimensional assumption, contact rank, pivot basis, fixed minor, or
coordinatewise-independence assumption occurs. Printed axioms are only
`propext`, `Classical.choice`, and `Quot.sound`. The file contains no `sorry`,
`admit`, `decide`, or `native_decide`.

```text
Lean SHA-256 7b95c61d13afd1abe9473bb644a8be37dd4ff7318d2add46fdf0d3d140183ead
compile      lake env lean -j1 -M8000 ...   4.4 s
```

This is stronger and safer than a fixed bordered-minor lemma: contact rank and
compatible-dual dimension may change arbitrarily; only intrinsic separation
by complete-kernel vectors matters.

## 4. Target m47 legality and capacity

The literal analogue of the minimum finite repair is

```text
{ X^j Y^48, X^j Y^48 Z : 0 <= j < 2,188,003 }.
```

The exact width is

```text
47*180413 - 131071*48 = 2,188,003.
```

It is legal in `(m,B,s,U,L)=(47,16,8,64,3757)`:

```text
R=S=0,                    so weighted derivative degree is 0 <= 16;
Y-degree=48 <= U=64;
Y-degree+Z-degree <=49 <= L=3757;
the strict X width is positive.
```

The pair has `2*2,188,003=4,376,006` coefficients. At one node, two complete
`X` bands have the elementary local rank cap `2m=94`. Even granting the
stronger favorable fiction that only the 81,731 error nodes charge rank,

```text
4,376,006 - 81,731*94 = -3,306,708.
```

Against all nodes the deficit is `-20,265,530`. Therefore this pair does not
force a standalone kernel. Its target use must be *relative to the packet*,
exactly as stated by `RELATIVE-KILLER`.

The full m47 source remains barely capacity-positive:

```text
columns                          65,061,789,117,960
one-node relaxed rank                   248,191,020
all-node margin                           2,371,080
```

But the natural cap envelope is knife-edge. Keeping `m=47,L=3757`, the three
one-cap-lower controls are all red:

```text
(B,s,U)=(15,7,64)    margin -108,395,787,462
(B,s,U)=(16,7,64)    margin     -701,011,338
(B,s,U)=(16,8,63)    margin  -27,189,454,746
```

The target-ratio falsifier above now says more: one may not even posit the
two-band relative recurrence from its m4 behavior. One cannot replace the
full source by it, nor casually discard the high derivative and active caps
which make the global dimension count green.

## 5. Exact remaining universal hypothesis

The finite result closes the mechanism question only in its chamber. Since
the packet-line premise itself fails in the target-ratio controls, the exact
target obligations must use the arbitrary-corank interface:

```text
1. PACKET-DUAL DESCRIPTION at arbitrary exact-G target data:
   identify the full packet-compatible boundary-dual subspace without fixing
   a contact pivot; its dimension may be at least three.

2. SEPARATING RELATIVE REPAIRS:
   construct enough legal complete-source raw combinations and packet
   combinations with equal complete contact traces that their boundary
   differences separate that whole compatible-dual subspace.
```

Both assertions must retain all nodes, all order-two contact coordinates, and
the arbitrary Newton quotient `T(X)`. The numeric vector `(1,8,2,1)` and the
finite `Y^5` pair are specializations, not universal formulas; the new exact
controls prove that neither their corank nor their repair transports merely
from the constant-`T` relation and target-like cap ratios.

In particular, the following shortcuts remain invalid:

```text
copy the two finite bands and invoke dimension                    RED
infer a target recurrence from one F_11 circuit                   RED
assume all-anchor packet compatibility is always a line           RED
use separate gains for Y,R,S,Z as independence                    RED
fix one maximal contact minor across mismatch strata              RED
```

The go criterion is a symbolic full-source coefficient/recurrence identity
proving both obligations above, possibly without using the packet at all. The
no-go criterion is an exact target-valid datum with a nonzero compatible
covector for the *complete* source. Until one of those is available, there is
no 6900 candidate or verifier-ready submission.
