# m69 active route: true-source attachment critic

Date: 2026-09-14 UTC.  Scope: adversarial read-only audit of commits
`b58b762`, `39c05e1`, `d4002f9`, `5f3f931`, `8a6f00a`, `e72b9c1`, and
`a491cbb`.  Production, candidate, score, and radius files are unchanged.

## Binary verdict

**The current m69 Pascal experiments are detached from the true source
caller.**  They contain useful conditional linear algebra, but no theorem
currently maps the literal m69 contact kernel for the original received pair
to their `W=N0/E0` fixed-shape map.  Commit `5f3f931` then attempts to attach
the wrong terminal object: `leaf.cross.C00` is not the Pascal terminal
coefficient.

This is a composition failure, not evidence that the m69 rank theorem is
false.  It is nevertheless a hard STOP: do not extend the cyclic/high-wrap
case census until one true-source block is derived definitionally.

Estimated end-to-end 6900 readiness is **10--15%**, with **85--90% remaining
uncertainty**.  There is no honest submission ETA.  The missing item is a new
source theorem, not mechanical Lean assembly; absent a quick attachment
GREEN, the route should be pivoted.

## 1. The actual caller

The available source producer is

```text
Order2PassiveSeedSource.exists_global_passive_universal_vanishing
```

in `OriginalPassiveSeedSource6900.lean:915`.  At the m69 profile its concrete
source and contact map must be

```text
S69(K) = PassiveSourceIndex
           (69*180413) 131071 94 24 10 2369 -> K,

C69(U) = globalPassiveArrayConstraint
           K Index 69 (69*180413) 131071 94 24 10 2369
           (94 <= 2369) domain (U 0) (U 1),

K69(U) = ker C69(U).
```

It reconstructs a nonzero

```text
Q : SeedPoly K = Polynomial (MvPolynomial (Fin 4) K)
```

whose passive-seed specialization vanishes on every actual graph
`P_gamma = U0 + gamma*U1` with enough agreements.  The pair in this theorem
is the original fixed pair `(U0,U1)`.  The exact expansion theorem

```text
localSeedSubstitution_passiveGlobalSourceMonomial_eq_sum
```

shows that a source `Y` exponent `k` contributes the scalar

```text
u0^(k-f-g) * u1^g * choose(k,f) * choose(k-f,g).
```

The m69 Pascal coefficient `choose(k,y)*W^(k-y)` is its top passive diagonal
`f=y`, `g=k-y`, where the multiplier is literally the source's second word
`u1`.  Current m69 modules substitute the leaf wedge

```text
W(i) = d(i) U0(i) - c(i) U1(i) = N0(i)/E0(i)
```

without a theorem that turns `C69(U0,U1)` into `C69(v0,W)`, or derives the
same formula after an actual boundary-dual contraction.  The DataEleven leaf
contains no selected polynomial family agreeing with `v0+gamma*W`.

The small compiled countergate
`M69PhysicalDirectionReplacementCountergate6900.lean` records only the
direct-reparameterization obstruction: if the same fixed affine graphs are
represented using `(u0,u1)` and `(v0,w)` at two distinct seeds, then
`w=u1` and `v0=u0`.  Two size-180413 agreement sets in 262144 nodes overlap
in at least 98682 nodes.  This does not refute an independently derived
boundary-dual transform; it makes that transform a mandatory theorem.

## 2. What the terminal `C_q` really is

For a source vector/Q and fixed derivative shape `(r,s)`, put

```text
yT = 94-r-s,
zT = 2369-94 = 2275.
```

The terminal polynomial `C(X)` is the **primal source coefficient polynomial**
of the monomial

```text
Y^yT R^r S^s seed^zT
```

inside Q.  Its strict X width is

```text
69*180413 - 131071*yT - 131070*r - 131069*s.
```

For `(r,s)=(0,0)` this is 127823.  After the local shift
`X -> node+contactZ`, the q-th target is the q-th Hasse coefficient

```text
(Polynomial.taylor node C).coeff q
  = (C.hasseDeriv q).eval node.
```

Thus the chain `(r,s,q)=(0,0,26)` sees a primal prefix of strict dimension
`127823-26=127797`.  In a transpose proof, a dual output tuple must annihilate
this prefix; `C_q` itself is not a dual boundary class.

No current Lean definition packages this `C(X)` from Q.  The nearest exact
source facts are:

* `reconstructPassiveSource_coeff`, which recovers one scalar coordinate;
* `passiveSourceIndex_caps`, which proves its support bounds;
* `localSeedSubstitution_passiveGlobalSourceMonomial_eq_sum`, which expands
  one source monomial.

The generic `terminalCokernelContact`, `gradedConnecting`, and
`gradedProjection` modules take abstract terminal spaces/maps from a caller;
they do not instantiate the terminal space with these coefficients of Q.
Likewise, the `boundary` in the exact-g CS4 files is caller-supplied.

Consequently `leaf.cross.C00` cannot be used as this terminal polynomial.
`C00` is `rowConstant A 0` from the primitive-conic/DataEleven construction,
has only the bound `degree <=156003`, and is neither a coefficient of Q nor a
Hasse extraction from Q.  The direct-C00 experiment in `5f3f931` is a useful
negative test, but it closes no source edge.

## 3. Is m69 trying to prove Q=0?

**No, not in the only coherent existing architecture.**  The Pascal maps are
primal terminal-shell correction maps.  Showing their terminal target is in
the lower-prefix contact image produces a complete-contact kernel vector with
a prescribed terminal coordinate.  Dually, it rules out a terminal contact
cokernel class.  This is input to terminal/graded Schur surjectivity and
ultimately the four-boundary CS4 rank gain; it does not show the source
polynomial Q is zero.

The relevant abstract call graph already in the repository is

```text
literal C69 : S69 -> complete contact target
  -> split lower-prefix/terminal contact                 [not instantiated]
  -> terminalCokernelContact or gradedConnecting         [abstract GREEN]
  -> terminal/graded correction lift                     [abstract GREEN]
  -> boundary : ker C69 -> Fin 4 -> K(X)                 [not defined for m69]
  -> NoNonzeroDualContactRelation / CoupledSchurGainFour [abstract GREEN]
  -> four common rows, root forcing, finite-family count [existing framework]
  -> selected-family bound / ProtocolClaim 6900.
```

The separate `NoDataElevenHighEClosedLeaf6900` theorem is only endpoint
plumbing.  Nothing maps an m69 kernel-row/CS4 result to negation of that leaf.

## 4. Single highest-information next gate

Freeze every new rank instance and prove one **literal m69 source projection
theorem** for `(r,s,q)=(0,0,26)`:

```text
projectedContact69_chain0026:
  the projection of C69(U0,U1) to the two deficient rows y=41,42,
  after an explicitly defined terminal/lower source split and any explicitly
  defined boundary-dual transform, equals the fixedShapeSourceOutput map,
  with terminal input hasseDeriv 26 of the actual Q coefficient C(X).
```

This theorem must start from `globalPassiveArrayConstraint ... (U 0) (U 1)`;
it may not start from an abstract `W` or an invented `C26`.  It must state how
the leaf fields `(c,d,E0,N0)` enter and prove every equality, including common
factor roots.

Kill criteria (one focused implementation attempt, not another census):

1. If `W` appears only by instantiating the source's `values1` argument with
   `W`, with no degree/agreement-preserving graph transform, **kill m69**.
2. If the intended transform is a boundary-dual contraction but no concrete
   `boundary : K69(U) -> Fin 4 -> K(X)` produces it, **kill the present route**.
3. If the terminal input cannot be definitionally/provably identified with
   `hasseDeriv 26` of the actual Q coefficient, **kill the current terminal
   containment claim**.
4. If the proof needs independently selectable `C_q`/`P_{k,q}` across q or
   chains despite their coming from shared coefficient polynomials, **kill
   the claimed confluence**.

A GREEN here earns continuation to universal rational/high-wrap work.  A RED
or still-abstract result earns an immediate pivot.

## 5. Endpoint correction and fallback

If a leaf-elimination route is retained, its correct weakest endpoint should
use

```text
ProjectiveHighDataElevenHighEClosedLeaf
```

rather than arbitrary `DataElevenHighEClosedLeaf`.  The already compiled
`target_bad_family_small_or_projective_high_dataEleven_leaf` produces this
stronger same-witness leaf, and it retains
`CanonicalHighTailDirectionIndependent`; the consumer exports canonical tail
rank two.  Demanding elimination of every weaker DataEleven leaf discards
useful information and may make the premise strictly harder than necessary.

If the literal source gate above is RED, the immediate fallback is therefore
to abandon m69-as-a-wedge-source and target the stronger projective-high leaf
directly.  If a source route is still desired, the existing m60 exact-g
framework is semantically attached to `(U0,U1)` and to root forcing; its
single honest mathematical blocker is CS4, not a missing source coordinate
change.  Neither fallback is close to submission today.

## 6. What the current GREENs actually buy

* `d4002f9`, `8a6f00a`, and `a491cbb`: exact rank/containment for special
  rational functions and one two-row chain only.
* `e72b9c1`: conditional polynomial confluence once its displayed
  `ConfluentRelation` hypotheses are supplied; no module derives them from a
  `FixedShapeSourceAnnihilator` or C69.
* `5f3f931`: correctly demonstrates that direct C00-prefix attachment is not
  available; it must not be read as a partial positive adapter.
* `7068012`/`a742238`: exact endpoint plumbing conditional on a universal
  no-leaf theorem; no source mathematics.

This is meaningful reusable algebra, but currently zero closed arrows from
the actual m69 source into the DataEleven endpoint.

## Verification

The countergate compiles directly with `lake env lean` and prints only the
standard allowed axioms (`propext`, `Classical.choice`, `Quot.sound`; the
arithmetic lemma needs only `propext`).  It contains no `sorry`, `admit`,
`decide`, `native_decide`, unsafe declaration, or generated table.
