# m69 post-zero-set route and process critic

Date: 2026-09-14 UTC.  Scope: lower target 6900 only.  This is a hostile
composition audit after commits `cb97180`, `1795b4b`, `34bcb23`, and
`da0db75`.  It changes no production source, score, candidate, or submission.

## Verdict

The m69 line is still the **highest-information** active route, but it is not
close to an endpoint.  It should receive one more sharply bounded experiment:
pair the literal actual four-output right-hand side on the normalized-numerator
zero set against the one-dimensional dual of the `C_127729` base prefix.

This gate is logically before simultaneous confluence.  It can kill the
current rational-prefix architecture using one deficient shape and one dual,
whereas a global confluence computation would organize 140,153 obligations
for a source map which may already be impossible on one zero set.

The current `AlignedSourceRankOneAt` conclusion is not enough to pass this
gate.  Rank one is only a pointwise algebraic relation; base-prefix membership
is a global Reed--Solomon parity condition.  The full source equations, common
polynomial provenance, and actual selected-family incidence have to supply the
missing parity identity.

No percentage or submission-readiness increase follows from the four commits.
They reduce uncertainty by closing two false shortcuts and validating two
mechanical premises.  At least the target-specific zero-set theorem,
simultaneous confluence, and the endpoint/source composition are still open.

## What the four commits actually establish

### `cb97180`: universal arbitrary-rational rank is false

For the deficient m69 shape `(y,r,s)=(0,0,0)`, the base fringe is `127729`.
An allowed normalized numerator can vanish on `127730` domain nodes.  On those
nodes every positive power of `W=N0/E0` vanishes, leaving a map of rank at most
`127729` into a `127730`-dimensional target.  The exact hostile ledger

```text
load=max(deg c,deg d)+deg Q = 0
deg E0 = 2151
deg N0 = 127730
127730 <= 131071+2151
```

satisfies the currently advertised degree, excess, root-free-denominator, and
coprimality premises.  Therefore no number of additional positive powers of
`W` repairs the unqualified theorem.

### `1795b4b`: the full leaf reaches only a pointwise target split

`high_E_closed_leaf_m69_rational_parameters` proves the exact full-leaf caps

```text
deg E0 < 18415,
deg N0 <= 149776,
deg B+deg N0+max(deg L,deg M) <= 156003,
deg B+2*max(deg c,deg d)+deg Q+max(deg L,deg M) <= 22883.
```

`high_E_closed_leaf_m69_nodal_target_split` then proves:

```text
B(i)=0                    => all four actual C-values are zero;
B(i)!=0 and N0(i)=0       => the received wedge is zero and the four
                             actual C-values satisfy AlignedSourceRankOneAt.
```

It does **not** prove a bound on the number of `N0` roots, base-prefix image
membership, an m69 correction, or a selected-bad-family count.  The existing
Live set has at least `237212` nodes, so a hostile `127730`-node set can fit
inside it.

The first-defect square theorem in the same commit is narrower still.  It is
GREEN only for the literal first defect when both rational degrees are at most
`129449`; the full leaf permits `deg N0` through `149776`.  Even closing the
middle channel on the remaining 20,327 degrees would not address the separate
low-fringe `(0,0,0)` obstruction.

### `34bcb23`: local columns are nonzero, not jointly allocatable

All 7,918,984 enumerated physical predecessor channels have nonzero literal
Hasse weights.  This validates the interpretation of each individual prefix
window.  It proves neither independence of overlapping windows nor the
simultaneous allocation of shared source variables across the 140,153
deficient physical coefficients.

### `da0db75`: bare scalar load cannot be forced positive

There is a kernel-checked scalar hard-corner plus projective-high model with

```text
load=max(deg c,deg d)+deg Q = 0
```

and `p^2` scalar records.  Its agreement sets are empty, so it is not a full
DataEleven leaf.  Actual agreement incidence excludes that particular plane:
two agreement sets intersect in at least `98682` nodes, while its scalar
differences have degree at most `2049`.

This shows exactly that a positive load theorem, if true for full leaves, must
use the actual agreement sets and a same-witness coupling to `(c,d,Q)`.  No
audited API contains that coupling.

## A new exact guard: pointwise rank one is insufficient

Let `Z={z_0,...,z_127729}` be any `127730` distinct domain nodes and put

```text
P_Z(X) = product_i (X-z_i),
lambda_i = 1 / P_Z'(z_i).
```

For every polynomial `f` of degree `<127729`, Lagrange interpolation gives

```text
sum_i lambda_i f(z_i) = 0.
```

Thus `lambda` spans the cokernel of the restricted `C_127729` prefix.  But

```text
sum_i lambda_i z_i^127729 = 1.
```

Now take the pointwise rank-one parameters `(c,d)=(1,0)` and the four values

```text
(C00,C10,C01,C11) = (X^127729,0,0,0).
```

All four equations in `AlignedSourceRankOneAt` hold, while the first output
has nonzero `lambda` pairing and is outside the base-prefix image.  Its degree
also lies below the full-leaf row-constant cap `156003`.

This is not asserted to be a full DataEleven leaf.  It is an exact
counterexample to the process inference

```text
pointwise rank-one + current degree caps => shared base-image membership.
```

Consequently an experiment which checks only the four rank-one identities is
guaranteed to return a false GREEN.  The next proof must substitute the
literal definitions of the four row constants/source equations and show that
their `lambda` pairing vanishes.

## Route comparison

| work item | present status | information value now | decision |
|---|---|---|---|
| Actual `W=0` four-RHS/base-prefix membership | One exact counter-obstruction; full leaf gives only pointwise rank one | Highest: one dual can kill or reveal the missing identity | **GO first** |
| Simultaneous m69 confluence | 6,930 shapes and 140,153 coefficient obligations; only individual monomial-window coverage and nonzero weights are proved | Essential, but downstream of the zero-set gate | **WAIT, then GO second** |
| Full-leaf `load>=202` incidence coupling | False from scalar record/high tail alone; actual agreement coupling is absent | Low unless a downstream consumer is named | **STOP as standalone route** |
| Accepted-6806 retarget/simplification | Earliest scalar and all six phase kernels fail at target agreement; old ledger and package also miss current limits | Already decisively audited | **STOP; reuse primitives only** |

### Why `load>=202` is not a repair of the zero obstruction

Even granting the desired lower bound, the abstract hostile arithmetic can be
retuned to

```text
load = 202,
deg E0 = 2353,
deg N0 = 127730,
202+2151 = 2353,
127730 <= 131071+2353 = 133424.
```

The same `127730` numerator zeros still annihilate every positive-power
channel.  Therefore `load>=202` has no current m69 receiver.  Reopen it only
if a theorem is stated which converts the actual incidence coupling directly
into either fewer than 127730 numerator zeros or base-prefix parity on those
zeros.

### Why accepted 6806 is not the pivot

The exact accepted audit already records:

```text
accepted selected-count excess over target MCA = 20291918785929576
accepted root excess over current 10 MiB cap   = 546304 bytes
smallest Phase05 target per-node rank saving   = 74580740964
Phase05 target count-minus-rank deficit        = 19550893759173647
```

The other five target phase deficits are larger, up to
`220090056863886727062784`.  These are failures before table assembly.  The
accepted proof remains useful for generic root-count/contact lemmas, but
transplanting phases, regenerating ledgers, or repacking files cannot create
the missing target kernel or erase the 20.29-quadrillion count excess.

## The single fastest discriminator

Do not build an all-node or all-strata matrix.  Fix the low-fringe deficient
shape `(0,0,0)` and work on

```text
ZN = {i | B(domain(i)) != 0 and N0(domain(i)) = 0}.
```

For the first decisive case `|ZN|=127730`:

1. construct the explicit RS dual `lambda_i=1/P_Z'(domain(i))`;
2. substitute the literal four `source00/source10/source01/source11` right-
   hand sides, not merely `AlignedSourceRankOneAt`;
3. use `d U0-c U1=0` and every full-leaf field that genuinely applies on
   `ZN`;
4. reduce the four shared-source pairings to symbolic polynomials;
5. accept only either an identity forcing all required pairings to zero, or a
   leaf-compatible assignment with one nonzero pairing.

This is a one-cokernel calculation.  It should be performed symbolically or
with a tiny exact faithful control before any Lean formalization.  A random
rank scan, dimension count, or check of the rank-one minors is not a result.

Interpretation:

* **nonzero pairing compatible with the full source fields:** kill the current
  m69 rational-prefix route and pivot to a source with an `N0`-independent
  channel or to a direct selected-family incidence count;
* **identical zero forced by the actual equations:** formalize that precise
  identity, extend it to arbitrary `|ZN|`, and only then start punctured rank
  plus confluence.

## Confluence work after a GREEN zero-set gate

The old m60 actual-residual result gives a useful pattern: correlated Pascal
residuals annihilate raw-contact left duals because each common coefficient
multiplies one intact raw contact column.  Its literal frozen-`u1` Hankel
minor, dimensions, and `M=60` profile do not transfer to m69.

For m69, first mechanically reprove the parameter-generic residual
factorization.  Then build only the smallest source-tagged pair of deficient
coefficients which reuse a predecessor variable.  Pair its actual RHS with
the smallest structural dual.  Continue to an SCC at a time only while every
dual pairing vanishes identically.  A first nonzero actual pairing is a route
STOP; an invertible local minor without source tags is not confluence.

Do not infer global solvability from:

```text
396574005663 source-count margin,
all individual monomial intervals covering,
all Hasse weights being nonzero,
or the first descending defect being solvable.
```

The margin is a nullity statement for one source map, not a fungible budget
for omitted nodes, cokernel dimensions, or repeated use of shared variables.

## Hidden composition gaps and duplicate work to avoid

1. The m69 defect census and reciprocal interval gate are executable source
   models.  `M69DataElevenRationalGate6900.lean` exports degree ledgers and a
   nodal target split.  There is no theorem in the audited chain that composes
   these into the literal m69 correction map and then into
   `SelectedBadGivenSetsBound`.
2. The low-fringe `(0,0,0)` shape is not the first descending defect
   `(A,n,H,j)=(63,44,24,14)`.  A GREEN first-defect middle-channel theorem
   cannot be reported as an all-profile advance.
3. Content/common-factor roots are a separate seam.  At actual `B` roots all
   four RHS values are already zero, while at live `N0` roots they need not be
   base-compatible.  Do not charge four arbitrary values at content roots or
   use that charge to hide the numerator-zero obstruction.
4. Hasse-weight nonvanishing is closed.  Do not factor those 2,115 weight
   polynomials again unless the field/profile changes.
5. The reciprocal-monomial exponent sweep is closed in its unit-numerator
   scope.  More exponents, more random rational functions, or a denser scan do
   not answer the actual zero-set parity question.
6. The accepted phase/table and first-defect-only branches have exact STOP
   receipts.  Reopening either requires a new premise and a stated endpoint
   consumer, not a smaller implementation.

## Scheduling and kill/continue rules

1. **Now:** one owner computes the actual `ZN` dual pairing.  Other work may
   formalize already found identities, but must not launch a full confluence
   scan.
2. **If GREEN:** one lane proves the arbitrary-`ZN` parity theorem; one lane
   constructs punctured rational rank on its complement; one lane starts the
   smallest source-tagged confluence SCC.
3. **If RED:** stop m69 rational-prefix work immediately.  Preserve the exact
   census and Hasse lemmas for any future source with a numerator-independent
   channel, and pivot the mathematical lane to direct actual-incidence
   counting.
4. **Never reopen scalar-load or accepted-6806 work** without an explicit new
   theorem whose conclusion feeds the zero-count/base-image gate or the exact
   target bad-family bound.

This order maximizes falsification speed.  It also makes the next positive
result compositional: a GREEN is credited only when it closes the exact
one-dual obstruction created by `cb97180`, rather than when it proves another
property compatible with that obstruction.
