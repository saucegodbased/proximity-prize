# Post-breakthrough process audit: Full187 lower 6900

Date: 2026-09-14 UTC.  Scope: commits `2d678e8`, `9192095`, `14b5109`,
`d35fcb4`, `396fa01`, `f2a46d5`, `9276998`, `500026a`, `91265f0`; the
stable uncommitted F101 actual-F3 script; the supplied uncommitted pure-slice
receipt; and discussion comment 18427358.  No production or solver file was
changed.

## Verdict

Scheduling is **conditionally honest, but only with an immediate
falsification-first reset**.  The evidence has found two genuine mechanisms:
target-exact adjacent-window/adjacent-slope pivots, and an exact small-field
two-sided factor module for the literal F3 packet.  It has not found a Full187
F3 source representative, a closed top-shell cascade, or a 6900 candidate.

The honest status remains:

```text
accepted production baseline supplied to this audit: 6806
Full187/6900 source containment:                    open
Full187 cross-slope exported tails:                 open
candidate/build/comparator/submission at 6900:      none
```

The nine-commit baseline was 6806.  The subsequently reported promotion of
6810 updates the accepted floor and reduces the numerical gap from 94 to 90.
It does not, from any evidence in this audit, discharge either open line
above.  Its rational path should be reopened for 6900 only upon a literal
target-6900 source/consumer inequality or a reusable theorem whose hypotheses
are checked at 6900; promotion or score proximity alone is no structural
transfer.

## What the chain actually establishes

1. `2d678e8` cancels 87.15% of terminal provenance by complete Hermite
   blocks.  Its 2,622,661 surviving occurrences are individually licensed,
   not simultaneously sectioned.
2. `9192095` and `14b5109` give exact full-row-rank first-fringe maps,
   including all eleven q26 classes.  They are local layer results; their
   finite fringes are reused by later equations.
3. `d35fcb4` is a valid finite counterexample to the inference “all adjacent
   first fringes pass, therefore every per-shape arbitrary `C` assembles.”
   Its “ratio-faithful” description does not make it a target counterexample.
4. `396fa01` closes only the first induced q26 sink and proves its boundary
   normal is zero.  It explicitly leaves the cross-shape capacity section.
5. `f2a46d5` and `9276998` correctly kill the bare passive-`Z` factor and all
   polynomial mixing **inside the separated 60-summand transfer family**.
   They do not kill a larger simultaneous/bicovariant module.
6. `500026a` proves a real C=1 second-fringe defect in the isolated
   `(11,10)` three-polynomial system.  `91265f0` removes that defect only
   after projecting to two named aggregate rows: the two q=0 controls give
   the automatic unitriangular block `[[I,0],[K,I]]`.
7. The outgoing burden in `91265f0` is load-bearing.  Each P59 pivot still
   exports about 109,174 nonprincipal u0-free occurrences and exactly
   4,083,068 positive-u0 occurrences.  Strictly later passive `Z` or
   error-support is an orientation, not a simultaneous section.

Thus the cross-slope GREEN supersedes the 7,797 defect only on its two
principal rows.  It does not supersede the warning from `500026a` that a
mechanical per-shape Pascal cascade fails.

## New actual-F3 bicovariant chamber

The audited pre-error-direction snapshot was stable across the three fresh
exact runs at SHA-256
`80d5aa782023d8fb29c983fa47fc44b2ebc2f7261130c4b6b660eaaf8088bef7`.
Later working-tree edits are outside this receipt.  The audited runs give:

```text
F101, X<=7, Z<=7: GREEN; rank 3123; 1768-term representative
F101, X<=6, Z<=7: RED; correction/augmented rank 2940/2941
F101, X<=7, Z<=6: RED; correction/augmented rank 2752/2753
```

This is materially better evidence than arbitrary-`C`: it uses the literal
`F3=B*(Y-Z*q_H)`, checks complete-contact zero, exact boundary, and every
small-chamber source inequality.  The one-step ablations show both shift
directions are genuinely used.

It is nevertheless **bounded pre-fidelity F101 evidence, not Full187
progress yet**:

- the successful relation comes from dense nullspace elimination, not a
  Popov theorem or scalable Popov certificate;
- it uses 1,802 shifted generators and both maximum shifts, so it is a sharp
  corner solution rather than a slack/robust chamber;
- the target ledger only counts an upper bound of 916,928 columns and checks
  three shape predicates; it never ranks the target module;
- raw target generators are acknowledged to be source-illegal, and checking
  one deleted top face is not a proof that the combined target representative
  satisfies every half-open X/active/passive window;
- the nonzero four-boundary determinant certifies independence of four F101
  boundary rows, while this calculation constructs only the F3 lift.

The decision label `GREEN_SPARSE_FACTOR_MODULE_LIFTS_ACTUAL_F3` is safe only
when prefixed by “in this F101 chamber.”  Calling it a target lift, a
four-packet lift, or a Popov construction would be a logical overclaim.  The
sharp ablations prove necessity only in the chosen rectangular generator
family, not among all legal nonrectangular modules.

## Pure-slice gate

The supplied necessary specialization

```text
K = B Y + sum_(n=2)^82 A_n Y^n,
Xi_G^(60-n) | A_n,
Xi_E^(60-j) | sum_n binom(n,j) A_n
```

has 441,597,347 source dimensions against 319,331,010 agreement plus
149,567,730 error conditions: a raw deficit of 27,301,393.  Ratio-faithful
N=64 analogues are RED for m=3 through 12.

This is the strongest current evidence against target scaling, but it is not
yet a target theorem: row counts need not be independent.  Conversely, it
cannot be waved away by mixed `R,S,Z` optimism if the specialization has
really been derived from every legal Full187 lift—those terms vanish on the
face.  An exact nonzero remainder/dual certificate on this face would stop
the Full187 F3-containment lane, not merely the current bicovariant ansatz.

## Discussion lemma: presently a scope mismatch

[Discussion comment 18427358](https://github.com/proximity-prize/proximity-prize/discussions/530#discussioncomment-18427358)
proves, informally, saturation and an exact valuation formula for the specific DVR sector
`C0^i C1^(N-i)(Y-v)^j` at an ordinary double point with a distinct third
branch.  That is conceptually consonant with covariant products, but it does
not presently fit the two-sided family:

- the F101 family uses order-1/2/3 agreement and error covariants, locator
  powers, boundary-normalized differences, and passive-seed shifts, not the
  stated two-conic sector;
- no equality with a complete Full187 local subquotient is known;
- the lemma's local t-adic lattice does not allocate global X degrees,
  half-open coefficient windows, passive-Z indices, or error constraints;
- its ordinary-branch/unit-determinant hypotheses are not checked uniformly
  over the target node classes.

It becomes relevant only after an explicit sector isomorphism **and** a
degree-compatible global filtration are proved.  Until then it is another
seductive scope mismatch and should not raise source-containment confidence
or be scheduled for formalization.

## Ranked next 30 minutes

### 1. Exact target pure-slice membership/dual gate (first 18 minutes)

Compress the displayed divisibility system by CRT/quotient structure and
test the literal target `B Y` coset.  Seek a checkable nonzero module
remainder or dual functional, not another dimension count and not more small
fields.

Stop/pivot:

- **RED:** augmented rank gain one, nonzero remainder, or explicit dual
  annihilating all legal corrections but not `BY`.  Freeze Full187 F3 source
  containment (including the bicovariant scaling lane) and pivot to the
  promoted rational/consumer architecture only if it has a target-6900
  receipt.
- **GREEN:** exact target membership on this necessary face.  Proceed to the
  first mixed `R/S/Z` graded face; do not yet claim a lift.
- **No target certificate in 18 minutes:** record the exact structured matrix
  and scalable certificate plan, then stop parameter scans.

### 2. Whole-frontier cross-slope tail gate (parallel; first receipt by minute 30)

Aggregate the first complete later-`Z` frontier exported by both P59 pivots,
including curvature collisions and the induced higher jets, and expose its
physical pivot graph/SCCs.  A third hand-picked row is low information; the
gate must include every same-key origin on the first frontier.

Stop/pivot:

- **RED:** a closed SCC with exact rank defect after all legal physical
  controls, or any backward dependency not decreasing a proved global
  filtration.  Freeze the cross-slope Pascal cascade.
- **GREEN:** every first-frontier block has an explicit legal unit/Popov
  section and all exports strictly advance one common well-founded order.
  Then extend one entire passive-Z band before reporting progress.
- Positive-u0 terms may pivot to a separate error-capacity sink only after a
  simultaneous section is exhibited; “zero on agreements” is insufficient.

## Evidence that would move end-to-end confidence

Substantial positive movement requires either (a) a target-field F3
representative with complete contact zero, exact boundary, and **all** literal
Full187 source windows certified, or (b) closure of an entire cross-slope
passive-Z band plus its positive-u0 connecting map using shared physical
coordinates.  F101 greens, local capacity margins, row-shape collisions,
unit pivots on two rows, and more score-nearby accepted submissions do not do
that.

Even (a) or (b) is source-side progress, not 6900 completion.  End-to-end
confidence moves decisively only when the four packets, consumer/count,
exceptions, and ownership assemble in Lean and a flat submission builds
within the 24 GiB/80-minute verifier limits.  No scoped evidence currently
supports raising the probability of that outcome beyond “specific mechanism
now worth one target falsifier.”
