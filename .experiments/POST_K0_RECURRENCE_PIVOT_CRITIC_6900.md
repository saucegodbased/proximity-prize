# Post-K0 recurrence pivot critic

Date: 2026-09-15 UTC.  Audit base: commits `2345ece`, `36b0506`, and
`833b8c3`.  Scope: lower target 6900 only.  This note changes no production,
candidate, claim, or submission file.

## Decision

**Freeze K0 as the primary route.**  K0 has an exact next *statement*, but no
remaining concrete construction whose present end-to-end closure probability
is above 20%.  The recent rank-four wording made the endpoint smaller; it did
not make the global correction four equations long.

The best independent information bet is now the strongest existing
same-witness endpoint:

```text
ProjectiveHighDataElevenHighEClosedLeaf
  -> False.
```

This is still hard research, not a near submission.  It ranks first because
`target_bad_family_small_or_projective_high_dataEleven_leaf` produces exactly
that object from the benchmark bad family, while the existing protocol
wrapper consumes its exclusion without needing a new source-to-family
adapter.  O3 and accepted-6810 reuse each have more unsupported semantic
arrows before reaching that caller.

Subjective scheduling posteriors, conditional on using only mechanisms
already represented in this repository, are:

| route | endpoint closure probability | reason for the range |
|---|---:|---|
| same-witness projective-high leaf | 10--18% | exact benchmark caller and strongest retained hypotheses, but no global no-leaf/cover theorem |
| K0 terminal route | 5--12% | two target-global producers remain and both concrete correction families just failed |
| maximal O3 | 3--8% | target arithmetic/low-codimension consumers exist, but actual cycle provenance and the height-three owner remain open |
| accepted-6810 retarget | 1--3% | every accepted P4 source is target-negative; Full187 does not inhabit the accepted fixed-carrier phase interface |

These ranges are not proof claims or ETAs.  In particular, the low-head K0
rank-four theorem itself may be true with probability above 20%; proving it
with the current artifacts would still not close K0 because the final-three
producer is separate.

## Why the latest K0 GREEN is not a route GREEN

The exact terminal consumer needs two independent target-global inputs.

### 1. Low-head boundary surjectivity

`K0LowHeadFourRowAnnihilator6900.
compatibleBoundaryDual_eq_zero_of_lowHead_annihilator` consumes a concrete
map

```text
annihilator : Dual(Source) -> Defect
```

and proofs that it kills `head.dualMap` and is injective on
`boundary.dualMap`.  No target instance of this map exists.  Commit `36b0506`
proves only that four local osculating carriers have an invertible fresh
boundary matrix, with determinant `2*h^173`.  It does not cancel their old
error-node head.

The two natural global realizations are now exact REDs:

* `2345ece`: keeping the four carrier shapes forces 44 Hasse jets at every
  error.  The cost is `44*81731 = 3,596,164`, while the largest coefficient
  window is only `590,583`.
* `833b8c3`: the common-residual/geometric recurrence leaves the source box
  at epsilon order six for the active lanes and order seven for the passive
  lane; the first active deficit is `80,210` or larger.

Nearby escape hatches are also closed: `baca5fb` falsifies full fresh-probe
CRT near capacity, `0fd0f80` rejects the projection-mismatched rank-three
splice, and `8c14210` finds zero overlap between source-positive profiles and
the literal all-node locator family.  The abstract `annihilator` interface is
therefore a specification, not a current construction.

### 2. Final-three source equations

Even after low-head boundary surjectivity, the actual terminal proof must
derive the three aggregate equations at epsilon orders `44,45,46` from the
literal cap-3757 source.  In
`K0PassiveGhostThreeMoment6900.depth47_lastThree_reversedMoments_eq_zero`,
those equations occur as the hypothesis `hsource`; no target theorem supplies
it.  The corrected finite m8 connecting-transpose computation validates the
consumer shape only.

Thus a K0 continuation must newly produce both

```text
(A) Surjective (boundary restricted to ker oldLowHead),
(B) the literal final-three aggregate source equations modulo old contact.
```

Calling (A) “only four rows” hid the all-node, 44-layer cancellation cost;
calling the m8 result “the mechanism” hid (B).  This is the stale optimism
that the latest two RED receipts expose.

## Independent route comparison

### Same-witness projective-high leaf: first

The honest caller is already compiled:

```text
target_bad_family_small_or_projective_high_dataEleven_leaf
  : bad family
    -> small OR Nonempty ProjectiveHighDataElevenHighEClosedLeaf.
```

Its endpoint sibling is the existing checked chain

```text
NoDataElevenHighEClosedLeaf6900
  -> SelectedBadGivenSetsBound
  -> Order2ProtocolBadFamily6900.protocolClaim6900_of_bad_family.
```

The mathematically weakest useful replacement retains the stronger input:

```text
NoProjectiveHighDataElevenHighEClosedLeaf6900 :=
  forall U Gamma agreement selected,
    ProjectiveHighDataElevenHighEClosedLeaf U Gamma agreement selected ->
      False.
```

A trivial sibling adapter from this proposition to `ProtocolClaim 6900` is
mechanical.  The hard edge is the proposition itself.  The leaf retains one
actual `U/Gamma/agreement/selected`, the full DataEleven rational cross, the
huge family, and projective-high received-tail rank.  That makes it a more
informative target than the weaker universal `NoDataEleven...` cut.

This route is not secretly solved.  The m69 rational-Pascal attempt does not
attach: the literal passive source direction is the original `U1`, whereas
the attempted recurrence uses the leaf wedge `N0/E0`; polynomial row shear
breaks both the affine pencil and the source cap.  Separately, the strong-
excess factor program still lacks a cumulative original-coefficient cover of
the unpaid nonlinear/singular loci.  Neither local tangent collapse nor
global scalar rank supplies that cover.

### Maximal O3: archive, do not schedule first

The usable consumer is conditional on an actual
`G2StandaloneO3SharedVogelAdapter.SharedVogelProjectionBudget`; the compiled
arithmetic in `shared_owner_charge_le` pays the result once supplied.  The
repository's prime-tower work proves a proper-cut hierarchy only through
horizontal codimension two.  The target gap is still the conjunction of:

1. constructing the punctured terminal and one shared live system from the
   same retained family;
2. constructing/deduplicating one shared effective cycle with the advertised
   projection weights; and
3. turning every selected-bearing all-contained height-three component into
   a sparse fibre or a fixed normalized residual with more than `131071`
   common zero nodes.

Local rational third jets do not imply (3); the scalable local-owner controls
already show that.  Hence O3's “one owner theorem” description compresses at
least three semantic joins and is stale as a proximity estimate.

### Accepted 6810 retarget: hard stop

The exact target substitution makes every accepted A/B/T/phase/scalar source
dimension nonpositive.  In particular the accepted scalar source margin
changes from `+3,464,475` to `-482,231,575`, and all ten accepted kernels have
zero natural-number nullity lower bound at agreement `180413`.

Full187 has a genuine positive source margin `+9,757,693`, but its output is
a seed polynomial whose four-variable specialization moves with `gamma`.
The accepted Higher phase fixes one P4 carrier before ranging over `Gamma`
and calls `PhaseKernelRealization.gap_le_finrank` and
`stateLocalRegularBoundOn_onePhase`.  No theorem converts the moving Full187
carrier into that fixed object, and the first-two-helper and scalar-curvature
coercions have exact countergates.  A direct Full187 row-to-accepted-A repair
also misses the box by `4,232,190`.

Therefore “reuse 70--80% of the accepted code” is a code-volume statement,
not proof-distance evidence.  The load-bearing producers have to be replaced.

## Single highest-information action

Attack exactly `NoProjectiveHighDataElevenHighEClosedLeaf6900`, on the
existing structure and the same witness, and reject any intermediate lemma
that cannot be applied to this type.

The first proof/falsification gate should be the unpaid nonlinear-factor
branch, because all easier leaf branches are already charged.  A successful
result must return one of the two outputs for the **original coefficient
tuples of the same `Good` family**:

```text
* a complete reduced coefficient-locus cover, including singular and
  isolated components, whose aggregate incidence cost is below
  90,982,740,402,923,737; or

* a nonidentity one-ground-coordinate eliminant of cumulative degree at most
  42,700,739 together with a paid identity-fibre branch.
```

Those are existing consumer thresholds, not a new architecture.  The binary
stop rule is equally important: if the proposed derivation uses only local
jet/tangent rank, projective tail rank, source surplus, or `AlignedCrossData`
without the huge retained-family/agreement fields, reject it immediately;
repository countercontrols already satisfy those weaker premises.

This action has the highest information throughput because a GREEN closes
the benchmark-facing mathematical cut directly, while a RED demotes the last
route with an exact same-witness caller.  More K0 recurrences, O3 finite
chambers, or accepted-profile scans would revisit mechanisms already stopped
without changing an endpoint edge.

## Scheduling rule

Do not assign another primary slot to K0 until a proposed artifact explicitly
defines a target cap-legal annihilator (or equivalent source correction) and
also names the theorem producing the final-three `hsource` equations.  A new
local determinant, dimension surplus, finite-rank GREEN, or abstract linear-
algebra wrapper earns zero route-proximity credit.

No route audited here is ready for a candidate, build, comparator, or remote
submission.  The accepted verified floor remains 6810.
