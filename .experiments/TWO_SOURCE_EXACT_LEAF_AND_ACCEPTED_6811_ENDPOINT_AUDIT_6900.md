# Two-source protocol versus the exact leaf and accepted 6811

Date: 2026-09-15 UTC. Scope: exact lower-6900 endpoint audit using only
in-repository formal artifacts. This is not a candidate or submission claim.

## Binary verdict

The two-source protocol improvement is **real but insufficient**. It does not
move an unproved list term into the MCA allowance:

```text
TwoSourceLambdaEndpoint6900.lambda_le                         GREEN
TwoSourceCompleteEndpoint6900.protocolClaim6900_of_bad_family GREEN
```

Thus the sole protocol premise is genuinely

```lean
SelectedBadGivenSetsBound IRSProfile.domain 131071 81731
  263836564062556785.
```

The larger allowance creates a useful exact-leaf refinement, but no existing
theorem closes the refined leaf. The first literal composition gap is a small
record/adapter omission; the first mathematical gap is the degree-`133225`
scalar fibre/source theorem.

## Exact reallocation

The old and new protocol splits have the same total capacity:

```text
total capacity                              274980728111395087
old scalar/list term                         20296107496734967
new proved two-source scalar/list term        11144164048838302
list saving / MCA gain                         9151943447896665
old MCA allowance                            254684620614660120
new MCA allowance                            263836564062556785
```

The new list term is not conditional geometry. It is proved by
`Order2TwoSourceScalarList131071.target_scalar_finite_list_card_le`, transported
to sixteen rows by `Order2TwoSourceScalarIRS131071`, and consumed by
`TwoSourceLambdaEndpoint6900.lambda_le`. The checked `.olean` endpoint stack is
present, and its recorded public axiom sets are the standard
`propext`, `Classical.choice`, and `Quot.sound` only.

## What it buys on the actual surviving family

A counterexample to the new selected-family bound has cardinality above the
new MCA allowance, so it automatically enters the right arm of the existing
frontier

```text
Gamma.card < 254684620614660120 OR weighted-low-pole leaf.
```

The sharper actual fixed-scalar filter then has the exact ledger

```text
new MCA allowance                            263836564062556785
sharp exceptions                                225006860771435
sharp retained family                        263611557201785350
old retained family                          253511670984674103
retained-family gain                          10099886217111247
```

This same-family refilter is already proved in
`TwoSourceSharpFixedScalarRetarget6900.exists_sharp_retained_fixed_scalar_family`.
The complete scalar theorem at degree `133224` gives

```text
scalar family cap                            262736488658745377
sharp retained family                        263611557201785350
headroom                                         875068543039973
```

and the checked bridge
`TwoSourceSharpFixedScalarW133224Bridge6900` therefore forces

```text
W >= 133225.
```

So this is a genuine mathematical improvement, not protocol bookkeeping.

## Exact-current-leaf join audit

The current
`ProjectiveHighDataElevenHighEClosedIncidenceLeaf` does not itself expose all
arguments of the sharp bridge. Following its projections gives the required
DataEleven/DataNine witness, conic, aligned cross, relative primality,
nonzero output pair, coefficient rank at most three, grade at most `24932`,
weighted fixed-scalar model, and the original degree/agreement/badness data.
The rank-three coefficient shape can be recovered by
`PrimitiveConicData.rank_three_line_seed_degree_le_two`.

Exactly one upstream fact was discarded when
`DataElevenWeightedLowPoleLeaf` was packaged:

```lean
leaf.conic.content.natDegree < 16000
```

`TargetWeightedLowPoleWindowFrontier6900` proves this fact on the same `A`
immediately before constructing the leaf, but the leaf structure has no field
for it. Consequently the present exact-leaf record cannot directly instantiate
`exists_sharp_retained_fixed_scalar_family_W133225`. Retaining this field in an
enriched leaf (or applying the sharp refilter before projection) is mechanical;
it is not a new mathematical theorem.

There is a second small adapter needed to retain the strongest upper endpoint.
The current W133225 bridge consumes `FixedScalarWeightedLowWindowCondition` and
returns `W <= 149776`. The exact high-E leaf separately has the stronger
same-cross witness `E0.natDegree < 18415`, which gives `W <= 149485`, but the
bridge does not currently refilter that stronger witness. Applying the same
sharp producer directly to the data inside
`FixedScalarWeightedNoAdjacentHardCornerCondition` preserves this bound.

After those two mechanical same-witness adapters, the exact formal target
would be

```text
263611557201785350 <= Good.card
Good injects into one fixed-centre scalar list
133225 <= W <= 149485.
```

This leaves `149485 - 133225 + 1 = 16261` scalar degrees. The earlier exact
high-E window contained `16366` degrees (`133120..149485`), so the proved
two-source scalar cutoff removes 105 degrees. The often-quoted 281-degree
window uses an additional high-window receiver which is only conditional in
the repository; it is not the current formal endpoint.

## First genuinely missing theorem

For the existing weighted scalar architecture, the first missing theorem is
already the adjacent layer:

```text
degree <= 133225 and 180413 agreements
  -> scalar family cardinality < 263611557201785350.
```

No proved bound meets it. The strongest proved theorem stops at degree
`133224`. The exhaustive in-repository audit of every parameter pair admitted
by the present weighted finite-prefix helper API finds no legal D55 helper at
`W=133225`; the closest rigorous margin is

```text
-439923397817.
```

Falling to D56 makes the complete ledger

```text
276527007046517868,
```

which exceeds the retained allowance by `12915449844732518`. This certifies a
stop for parameter retuning inside that helper family, not an impossibility of
all W133225 arguments.

A precise alternative sufficient theorem is the top-coefficient concentration
producer recorded by the endpoint audit: choose one coefficient value
`beta0` and prove that the off-fibre mass is at most

```text
875068543039973.
```

Then the existing W133224 theorem bounds the main fibre and closes W133225.
After paying the old `p * 1203` nonidentity charge, the corresponding
identity/uncovered allowance is `872505303201074`. No existing theorem supplies
either mass bound; the checked countercontrols allow multiple unrestricted
identity fibres.

## Accepted-6811 comparison

The accepted 6811 selected-count ledger does not fit the new MCA allocation
even if every entry were unrealistically unchanged:

```text
accepted ledger maximum                     274693043573515013
new two-source MCA allowance                 263836564062556785
remaining miss                                10856479510958228
```

More decisively, the accepted machinery fails before its ledger. At target
agreement `180413`, its scalar source is short by `508361706` coefficients,
principal kernel A is short by `139976078005509` coefficients (minimum rank
drop `533966363`), kernels B and T fail independently, and all seven native
phase sources are negative. A larger downstream MCA allowance cannot create
those kernels. The accepted geometry and packing APIs become reusable only
after a genuinely target-valid source is proved.

## Scheduling conclusion

1. Preserve `content.natDegree < 16000` and the high-E witness in one enriched
   same-family adapter; this is bounded mechanical work.
2. Do not mistake that adapter for endpoint closure.
3. Treat W133225 top-fibre mass/source production as the first mathematical
   discriminator. Existing weighted-helper retuning is exhausted.
4. Do not replay accepted-6811 tables until a target-positive source exists;
   the new allowance reduces the hypothetical ledger miss but does not repair
   any source failure.

