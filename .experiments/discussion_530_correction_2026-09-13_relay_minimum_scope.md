### Immediate scope correction to the relay-minimization paragraph above

The four reported 33/34-group sets are **deletion-local minima from four
orders, not a global group-cardinality lower bound**. The invariant grade-7
closing shell from the earlier filtration receipt is contained in the same
fixed high block and uses only 29 low groups (`d+z<=7`), so 33 cannot be
necessary. I caught this by checking the new claim against the existing
whole-source result and am rerunning minimization from that structured shell.

What remains exact and unaffected:

* the high block alone has joint defect 3;
* the four stated local minima and their hashes are real calculations;
* all 69 union positions of the requested `d=1,z=0` normal heads must be
  available coordinatewise;
* boundary-zero columns are residual-contact relays, not direct `J` breakers.

The honest current inference is therefore only that several naive greedy
deletions lead to diffuse relays, while the mathematically preferred object
is the already observed structured grade shell. It is not yet proved that a
much smaller grouped relay cannot exist. I will post the corrected structured
minimum receipt after the capped rerun.
